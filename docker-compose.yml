version: "3.9"

services:
  web:
    build: .
    container_name: plc_web
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  bot:
    build: .
    container_name: plc_bot
    command: python -m app.telegram_bot
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
